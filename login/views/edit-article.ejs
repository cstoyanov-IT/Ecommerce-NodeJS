<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Modifier l'article</title>
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <div class="container">
    <h1>Modifier l'article</h1>
    <a href="/admin/panel">Retour au panneau d'administration</a>

    <form action="/admin/update-article/<%= article.code %>" method="post" enctype="multipart/form-data">      <label for="nom">Nom:</label>
      <input type="text" id="nom" name="nom" value="<%= article.nom %>" required>

      <label for="description">Description:</label>
      <textarea id="description" name="description" required><%= article.description %></textarea>

      <label for="image">Image:</label>
      <input type="file" id="image" name="image" accept="image/*">
      <img src="<%= article.image.replace('uploads/', '/uploads/') %>" alt="<%= article.nom %>" width="200">

      <label for="prix">Prix:</label>
      <input type="number" id="prix" name="prix" min="0" step="0.01" value="<%= article.prix %>" required>

      <label for="quantite">Quantité:</label>
      <input type="number" id="quantite" name="quantite" min="0" value="<%= article.quantite %>" required>

      <input type="submit" value="Enregistrer">
    </form>
  </div>
</body>

</html>

